Enlaze a github: https://github.com/nruiz209487/yt.python.git
1. Descargar e instalar Python 3.13
2. Marcar "Add Python to PATH"
3. Verificar:
    python --version
    pip --version
4. Crear entorno virtual:
    python -m venv venv
5. Activar entorno:
    venv\Scripts\activate
6. Instalar Kivy:
    pip install "kivy[full]"
7. Instalar yt-dlp (Python module):
    pip install yt-dlp
8. Probar Kivy:
    python -m kivy
9. Ver versión de yt-dlp:
    yt-dlp --version
pip install python-dotenv
pip install beautifulsoup4 requests
pip install lxml
pip install selenium
descargar el wdgewwebdriver ponerlo en venv y hacer referencia al .exe en env
Descargar ffmeg y ponerlo en la carpeta venv (no se crea hasta que cress el entorno ) https://github.com/BtbN/FFmpeg-Builds/releases puedes poner el .exe en cualquier carpeta pero cambia la ruta en el env
GIt: